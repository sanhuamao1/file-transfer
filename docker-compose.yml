services:
  codelink:
    build: .
    container_name: codelink
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - ACCESS_CODE=${ACCESS_CODE:-123456}
    volumes:
      - ./files:/app/files
      - ./data:/app/data
